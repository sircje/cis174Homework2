@model IEnumerable<CIS174_TestCoreApp.Models.Achievements>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FamousPeopleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.doa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FamousPeopleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AchievementsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AchievementsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AchievementsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
